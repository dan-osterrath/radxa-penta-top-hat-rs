[package]
name = "radxa-penta-top-hat-rs"
version = "1.0.1+local.2"
edition = "2024"
license = "MIT"
description = "Low-memory Rust control daemon for the Radxa Penta SATA HAT top board"

[dependencies]

[profile.release]
opt-level = "s"
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
